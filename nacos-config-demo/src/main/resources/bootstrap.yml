spring:
  application:
    name: nacos-config-demo
  cloud:
    nacos:
      config:
        server-addr: 172.2.0.105:8848
        namespace: cloud-demo
        file-extension: yaml
        shared-configs:
          - data-id: application.yaml
            refresh: true